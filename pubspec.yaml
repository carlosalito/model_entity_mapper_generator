name: model_entity_mapper
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.0-171.0.dev
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  source_gen: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner: ^2.5.4
  source_gen: ^2.0.0
  analyzer: ^7.5.4

flutter: